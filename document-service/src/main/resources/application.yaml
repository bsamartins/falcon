logging:
  level:
    root: INFO
    org.springframework: INFO
    org.springframework.cloud: INFO
    com.bsamartins.falcon.documentservice: INFO
spring:
  cloud:
    stream:
      rabbit:
        bindings:
          documents:
            consumer:
              exchangeType: topic
          documents-created:
            producer:
              exchangeType: fanout
